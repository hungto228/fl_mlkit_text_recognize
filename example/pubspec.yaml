name: example
description: Demonstrates how to use the fl_mlkit_text_recognize plugin.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:

  camera: ^0.9.2+2
  fl_mlkit_text_recognize:
    path: ../
  flutter:
    sdk: flutter
  flutter_curiosity: ^3.3.2
  flutter_waya: ^1.15.0
  permission_handler: ^8.1.4+2

dev_dependencies:
  flutter_lints: ^1.0.4

flutter:
  uses-material-design: true
